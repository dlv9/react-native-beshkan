import {StyleSheet} from 'react-native';
import {GetPrimaryColor} from '../../theme/colors';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: GetPrimaryColor('main'),
  },
});
